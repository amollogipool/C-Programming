#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	char ch='A';
	char plus_sign='+';
	int x=10;
    const int p=10;// The Value of constant variable can't be change during execution.
	float z=10.00;
    int true;
    int _seven=-7; //Except 0, All Nagative or Positive values are allowd.
    //ch>x
    //plus_sign>x
    //++y
	if(ch){
		//++ch;
		//++x;
		//++p;
		printf("hellow:%c",ch);
	}
	return 0;
}
